class Item(self, name, price, quantity):
    def __init__(self, name, price, quantity):
        self.name = name
        self.price = price
        self.quantity = quantity

    def add_quantity(self, quantity):
        self.quantity += quantity


class VendingMachine:
    def __inii__(self, items)
        self.items = items
        self.money_inserted = 0.0

    def displayProducts()
    def selectProduct(int product)
    def insert_money(float amount)
    def insert_card(int card_number)
    def card_transation()
    def dispense_item(item)
    def dispense_change()

    def controll()
        #you need to insert a password to acsess this method which contains:

        #method to add items
        #method to delete items
        #method to add quantities